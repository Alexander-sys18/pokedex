node_modules
.next
.git
.github
coverage
npm-debug.log*
.pnpm-debug.log*
.env*
!.env.example
README.md
Dockerfile
.dockerignore
.claude
tsconfig.tsbuildinfo
# NOTE: src/data/pokedex.generated.json ships IN the context on purpose —
# the committed snapshot makes `docker compose up --build` deterministic and
# independent of PokéAPI availability at build time.
